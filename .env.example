# PRISM Configuration
# Copy this file to .env and fill in your actual values
# Never commit the .env file - it's automatically gitignored

# === Context Memory ===
# Human-readable markdown notes in Obsidian vault
#
# Configuration Priority:
# 1. Environment variables (this file) - highest priority
# 2. core-config.yaml settings
# 3. Built-in defaults
#
# Path is relative to .prism folder
# Default: ../docs/memory → creates docs/memory at project root (same level as .prism)
#
# Example structure:
#   project-root/
#   ├── .prism/              (Claude plugin folder)
#   └── docs/
#       └── memory/          (Obsidian vault)
#
# Uncomment to override core-config.yaml:
# PRISM_OBSIDIAN_VAULT=../docs/memory
# PRISM_MEMORY_AUTO_CAPTURE=true

# === Jira Integration (Optional) ===
# Get API token from: https://id.atlassian.com/manage-profile/security/api-tokens
# JIRA_EMAIL=your.email@company.com
# JIRA_API_TOKEN=your-jira-api-token-here

# All credentials stored locally and never shared
