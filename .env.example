# PRISM Jira Integration Configuration
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit the .env file - it's automatically gitignored

# Jira Authentication (Required for Jira integration)
# Get your API token from: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_EMAIL=your.email@company.com
JIRA_API_TOKEN=your-jira-api-token-here

# Note: These credentials are read by core-config.yaml and used by PRISM agents
# to fetch Jira issue context. All credentials are stored locally and never shared.
